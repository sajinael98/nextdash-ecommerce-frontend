import { ArrayFieldTemplateItemType } from "@rjsf/utils";
import React from "react";

const ArrayFieldItemTemplate: React.FC<ArrayFieldTemplateItemType> = (
  props
) => {
  const {} = props;
  return <div>ArrayFieldItemTemplate</div>;
};

export default ArrayFieldItemTemplate;
